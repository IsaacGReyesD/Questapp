import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sigo probando pero con otro lenguaje',
  webDir: 'dist'
};

export default config;
